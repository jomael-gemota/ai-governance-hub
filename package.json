{
  "name": "ai-governance-hub",
  "version": "1.0.0",
  "private": true,
  "description": "AI Governance Hub - monorepo (frontend + backend)",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "build": "npm install --prefix backend --omit=dev && npm install --prefix frontend --include=dev && npm run build --prefix frontend",
    "start": "node backend/src/index.js"
  }
}
